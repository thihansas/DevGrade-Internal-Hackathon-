version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm test -- --reporters=default --reporters=jest-junit

artifacts:
  files:
    - test-result.xml
